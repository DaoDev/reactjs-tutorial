import React from 'react';
import PropTypes from 'prop-types';
import './index.scss'
Header.propTypes = {

};

function Header(props) {
    return (
        <div>
            <h1>Header</h1>
        </div>
    );
}

export default Header;